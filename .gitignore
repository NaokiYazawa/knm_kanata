node_modules/
.wrangler/
dist/
.env.local
.dev.vars
*.log
.DS_Store
.secrets/
projects.json

# 実装計画は GitHub に置かず、Worker から配る (docs/plans.md)。
# **先頭の / を落とさない** — `plans/` と書くと `src/plans/` (実装そのもの) まで無視される。
/plans/
