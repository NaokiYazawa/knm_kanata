{
  "mcpServers": {
    "kanata": {
      "type": "http",
      "url": "${KANATA_URL:-https://kanata.linto-dev.workers.dev}/mcp",
      "headers": { "Authorization": "Bearer ${KANATA_TOKEN}" },
      "timeout": 600000
    }
  }
}
